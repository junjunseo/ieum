# 위반: data -> ui 역방향 의존 한 줄 추가
module data depends ui {
  fn save() {
  }
}
module service depends data {
  fn handle() {
    call save()
  }
}
module ui depends service {
  fn main() {
    call handle()
  }
}
layer ui above service
layer service above data
