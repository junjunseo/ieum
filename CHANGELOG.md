# 변경 기록

## 미출시

### 추가

- 대표 예제 6종과 3단계 데모의 SVG·오프라인 그래프 뷰어 및 재생성 도구
- 정상→위반→수정 데모의 실행 차단·복구 검증과 로그 저장
- 초기 평가 입력 15개, 기대 위반 종류·건수 manifest와 평가 실행 도구
- 평가 오류 처리 및 그래프 산출물 일치 검증 8개와 Python 선택 테스트 경로
- F4 평가·가독성 기준 초안과 3분 시연 가이드

- `{}` 모듈 본문과 `let`, `fn`, `call` 문법
- 모듈 변수, 함수, 매개변수, 지역 변수와 호출 문장 AST
- 함수 문장 순서와 각 선언·문장의 소스 행 보존
- F1 문법 문서와 모듈 본문 정상 예제
- 이름·Scope, 함수 대상, 인자 개수와 재귀 호출을 검증하는 의미 분석기
- 현재 모듈 우선 및 직접 `depends` 모듈 기반 함수 해석
- 검증된 unit 함수 호출을 실행하는 최소 인터프리터와 실행 Trace
- `ieum <source> --run <module>.<function>` 실행 인터페이스
- 의미 분석 정상·위반 예제와 모듈 간 실행 예제
- `--emit-dot <파일>` 의존·계층 그래프 내보내기
- 순환·계층 위반 의존 경로와 잘못된 선언의 DOT 강조
- 선언 순서와 무관한 결정적 그래프 출력 및 대표 예제 6종 스냅샷 검증
- 그래프 파일 저장 실패가 구조 검사 결과를 바꾸지 않는 실패 격리
- Parser 46개, Pipeline 35개, Checker 35개, Graph 14개, Semantic 26개, Interpreter 15개로 총 171개 assert 테스트

### 현재 제한

- 리터럴, 대입, 연산, 반환문과 제어 흐름은 아직 지원하지 않음
- 모든 값은 unit으로 취급하며 진입 함수는 매개변수를 받을 수 없음
- 함수 호출은 이름으로만 해석하므로 같은 이름이 여러 직접 의존 모듈에 있으면 모호한 호출로 거부함
- 종료 조건을 표현할 수 없어 재귀 호출은 실행 전에 거부함

## 0.1.0 - 2026-08-10

### 추가

- 모듈 선언, 의존 선언, 계층 선언을 처리하는 Lexer, Parser, AST, Checker, CLI
- 중복 모듈, 미선언 계층 모듈, 자기 계층 선언 검증
- 미정의 의존 대상, 순환 의존, 직접·전이적 계층 위반 검증
- `ieum --version`을 통한 PoC 버전 확인
- 정상 1종·위반 5종 예제의 종료 코드와 대표 진단 자동 검증
- Windows·Ubuntu CMake/CTest CI

### 검증 기준

- Parser 20개, Pipeline 16개, Checker 31개 assert 테스트
- 정상 예제는 종료 코드 `0`과 성공 진단을 반환
- 위반 예제 5종은 종료 코드 `1`과 해당 위반 진단을 반환

### 알려진 제한

- 모듈 본문, 변수, 함수, 제어 흐름을 아직 지원하지 않음
- 실제 함수 호출을 기반으로 의존을 추론하지 않음
- 인터프리터와 의존 그래프 시각화는 이후 릴리스 범위임

이 버전은 이음 구조 검사 코어의 첫 공식 PoC 릴리스입니다.
